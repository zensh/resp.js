'use strict';
/*global describe, it, before, after, beforeEach, afterEach*/

var should = require('should'),
  resp = require('../index.js'),
  stream = require('stream');

describe('resp.js', function () {
  it('resp.stringify()', function (done) {
    // TODO
    done();
  });
});
